## Setup

Install dependencies:

```bash
npm install
```

## Running Tests

To run tests for a specific file, use the following format:

```bash
npm test <path-to-test-file>

# Ex.
npm test 1/checkBrackets.test.ts
```

To run all tests in the project at once, use:

```bash
npm run test:all
```
